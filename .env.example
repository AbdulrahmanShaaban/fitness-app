# Backup & sync (optional — the app works fully offline without these).
# Create a Supabase project, run sync/supabase-schema.sql in its SQL editor,
# then copy this file to .env and fill in the values from Project Settings > API.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=